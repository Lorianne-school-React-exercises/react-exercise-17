// create a MovieContext variable for our context, setting its initial value to null. don't forget to import createContext from react!
import { createContext } from "react";

const MovieContext = createContext(null);

export default MovieContext;
